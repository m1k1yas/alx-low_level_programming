#include<stdio.h>
/**
 * main - entry point for the program
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int i;

for (i = 0; i <= 9; i++)
{
        printf("%i", i);
}
putchar(10);
return (0);
}
