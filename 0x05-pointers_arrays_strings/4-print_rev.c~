#include "holberton.h"

void print_rev(char *s)
{
int len; 
int index;
char *start_ptr, *end_ptr, temp;

len = strlen(str);

start_ptr = str;
end_ptr = str+len-1;

for (index = 0; index < len / 2; index++) {


temp = *end_ptr;
*end_ptr = *start_ptr;
*start_ptr = temp;
start_ptr++;
end_ptr--;
}
}
